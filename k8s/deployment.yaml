apiVersion: apps/v1
kind: Deployment
metadata:
  name: nneyo0fp-4410-staging
  namespace: qb-prototypes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nneyo0fp-4410-staging
  template:
    metadata:
      labels:
        app: nneyo0fp-4410-staging
    spec:
      imagePullSecrets:
      - name: qtn-dockerhub
      containers:
      - name: web
        image: quantumteknologi/nneyo0fp-4410-staging
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
        env:
        - name: WATCHPACK_POLLING
          value: "true"
